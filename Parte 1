#include <stdio.h>
#include <stdbool.h>
#include <unistd.h>
#include <string.h>
#include <dirent.h>

#include <sys/types.h>
#include <sys/stat.h>

#include <errno.h>

#define TAM_BUFFER 512

int main ( int argc , char *argv[]){

char *listaArchivos[] = (char**)(malloc(sizeof(char *) * argc));
output = argv[2]; //Resultado.dat
DIR *d;
struct dirent *dir;
d = opendir(argv[1]); //directorio
int  i = argv[3]; //Pos1
int j= argv[4]; //Pos2

if ( i == 0){
	seekdir(0);
}else{
	seekdir(i-1);
}

while ((dir = readdir(d) != NULL && i <= j)){
	if( symlink_exists(
	
	
	i++;

}
closedir(d);
return(0);
}
